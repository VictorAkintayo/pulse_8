import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Pulse-8 CRM | Enterprise-Grade CRM Platform",
  description: "Enterprise-grade, metadata-driven CRM platform. Configure forms, automations, and workflows without code. Real-time updates, multi-tenant, and fully customizable.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}

